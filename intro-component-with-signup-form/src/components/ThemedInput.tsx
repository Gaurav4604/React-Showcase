import { TextField } from "@mui/material";
import React from "react";

type Props = {};

const ThemedInput = (props: Props) => {
  return <TextField placeholder="First Name" id="form-field" />;
};

export default ThemedInput;
